<?xml version="1.0" encoding="utf-8"?>
<!-- 
  REFERENCE FILE — not a complete AndroidManifest.xml
  After running `npx react-native init PluralSpace`, locate:
    android/app/src/main/AndroidManifest.xml
  
  Add these permissions inside the <manifest> tag:
-->

<!--
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
    android:maxSdkVersion="32" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
    android:maxSdkVersion="29" />
  <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

  In the <application> tag, confirm these attributes are present:
    android:label="Plural Space"
    android:allowBackup="false"
    android:theme="@style/AppTheme"

  The package attribute on the root <manifest> tag should be:
    package="com.pluralspace.app"
-->
